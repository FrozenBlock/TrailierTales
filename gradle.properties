# Increases the amount of memory Gradle can use and optimizes the GC
	# -XX:+UseZGC
	org.gradle.jvmargs=-Xmx4G
# Helps with build times
	org.gradle.parallel=true
# Caches Dependencies for faster build times.
	org.gradle.caching=true

# Fabric Properties
	# Get new versions at https://fabricmc.net/develop and https://lambdaurora.dev/tools/import_quilt.html
	minecraft_version=23w16a
    minecraft_json_version=1.20-alpha.23.16.a
	loader_version=0.14.19

# Mod Properties
	mod_version = 1.0.0
	mod_loader = Fabric
	maven_group = net.lunade.onetwenty
	archives_base_name = Luna1.20

# Dependencies
	fabric_version=0.78.1+1.20
